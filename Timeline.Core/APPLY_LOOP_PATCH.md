# Timeline Loop Feature

Interpolable.cs already contains the loop data model.

Apply `timeline_loop.patch` to `Timeline.Core/Timeline.cs`:

```
git apply Timeline.Core/timeline_loop.patch
```

Or replace Timeline.cs with the full version from the artifacts/conversation.
